/*
 * Created on 2021-09-27 ( Time 22:53:19 )
 * Generated by Telosys Tools Generator ( version 3.0.0 )
 */
package com.source.library.web.controller;

import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

import com.source.library.bean.OrderDataLog;
import com.source.library.business.service.OrderDataLogService;
import com.source.library.web.common.AbstractController;
import com.source.library.web.common.FormMode;

/**
 * Spring MVC controller for 'OrderDataLog' management.
 */
@Controller
@RequestMapping("/orderDataLog")
public class OrderDataLogController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "orderDataLog";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "orderDataLog/form";
	private static final String JSP_LIST   = "orderDataLog/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/orderDataLog/create";
	private static final String SAVE_ACTION_UPDATE   = "/orderDataLog/update";

	//--- Main entity service
	@Resource
    private OrderDataLogService orderDataLogService; // Injected by Spring
	//--- Other service(s)

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public OrderDataLogController() {
		super(OrderDataLogController.class, MAIN_ENTITY_NAME );
		log("OrderDataLogController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------

	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param orderDataLog
	 */
	private void populateModel(Model model, OrderDataLog orderDataLog, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, orderDataLog);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of OrderDataLog found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<OrderDataLog> list = orderDataLogService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new OrderDataLog
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		OrderDataLog orderDataLog = new OrderDataLog();	
		populateModel( model, orderDataLog, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing OrderDataLog
	 * @param model Spring MVC model
	 * @param orderDataLogId  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{orderDataLogId}")
	public String formForUpdate(Model model, @PathVariable("orderDataLogId") Integer orderDataLogId ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		OrderDataLog orderDataLog = orderDataLogService.findById(orderDataLogId);
		populateModel( model, orderDataLog, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param orderDataLog  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid OrderDataLog orderDataLog, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				OrderDataLog orderDataLogCreated = orderDataLogService.create(orderDataLog); 
				model.addAttribute(MAIN_ENTITY_NAME, orderDataLogCreated);

				//---
				return redirectToForm(httpServletRequest, orderDataLog.getOrderDataLogId() );
			} else {
				populateModel( model, orderDataLog, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			populateModel( model, orderDataLog, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param orderDataLog  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid OrderDataLog orderDataLog, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				OrderDataLog orderDataLogSaved = orderDataLogService.update(orderDataLog);
				model.addAttribute(MAIN_ENTITY_NAME, orderDataLogSaved);
				//--- Set the result message
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, orderDataLog.getOrderDataLogId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, orderDataLog, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, orderDataLog, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param orderDataLogId  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{orderDataLogId}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("orderDataLogId") Integer orderDataLogId) {
		log("Action 'delete'" );
		try {
			orderDataLogService.delete( orderDataLogId );
			//--- Set the result message
		} catch(Exception e) {
		}
		return redirectToList();
	}

}
