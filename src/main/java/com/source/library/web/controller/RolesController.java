/*
 * Created on 2019-03-14 ( Time 16:44:08 )
 * Generated by Telosys Tools Generator ( version 3.0.0 )
 */
package com.source.library.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

import com.source.library.bean.Members;
import com.source.library.bean.Roles;
import com.source.library.business.service.MembersService;
import com.source.library.business.service.RolesService;
import com.source.library.web.common.AbstractController;
import com.source.library.web.common.FormMode;

/**
 * Spring MVC controller for 'Roles' management.
 */
@Controller
@RequestMapping("/roles")
public class RolesController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "roles";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "roles/form";
	private static final String JSP_LIST   = "roles/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/roles/create";
	private static final String SAVE_ACTION_UPDATE   = "/roles/update";

	//--- Main entity service
	@Resource
    private RolesService rolesService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private MembersService membersService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public RolesController() {
		super(RolesController.class, MAIN_ENTITY_NAME );
		log("RolesController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Members"
	 * @param model
	 */
	private void populateListOfMembersItems(Model model) {
		List<Members> list = membersService.findAll();
		model.addAttribute("listOfMembersItems", list ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param roles
	 */
	private void populateModel(Model model, Roles roles, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, roles);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfMembersItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfMembersItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Roles found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Roles> list = rolesService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Roles
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Roles roles = new Roles();	
		populateModel( model, roles, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Roles
	 * @param model Spring MVC model
	 * @param roleId  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{roleId}")
	public String formForUpdate(Model model, @PathVariable("roleId") Integer roleId ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Roles roles = rolesService.findById(roleId);
		populateModel( model, roles, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param roles  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Roles roles, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Roles rolesCreated = rolesService.create(roles); 
				model.addAttribute(MAIN_ENTITY_NAME, rolesCreated);

				//---
				return redirectToForm(httpServletRequest, roles.getRoleId() );
			} else {
				populateModel( model, roles, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			populateModel( model, roles, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param roles  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Roles roles, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Roles rolesSaved = rolesService.update(roles);
				model.addAttribute(MAIN_ENTITY_NAME, rolesSaved);
				//--- Set the result message
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, roles.getRoleId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, roles, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, roles, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param roleId  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{roleId}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("roleId") Integer roleId) {
		log("Action 'delete'" );
		try {
			rolesService.delete( roleId );
			//--- Set the result message
		} catch(Exception e) {
		}
		return redirectToList();
	}

}
